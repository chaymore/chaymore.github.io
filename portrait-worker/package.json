{
  "name": "caleb-portrait-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute portrait-context --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute portrait-context --remote --file=schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260920.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.38.0"
  }
}
